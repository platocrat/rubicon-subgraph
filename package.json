{
  "name": "rubicon-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ platocrat/rubicon-subgraph",
    "create-local": "graph create --node http://localhost:8020/ platocrat/rubicon-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ platocrat/rubicon-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 platocrat/rubicon-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.3",
    "@graphprotocol/graph-ts": "^0.22.1"
  }
}